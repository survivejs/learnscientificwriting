diff --git a/plugins/htmlisp-renderer/mod.js b/plugins/htmlisp-renderer/mod.js
index 0000000..0000000 100644
--- a/plugins/htmlisp-renderer/mod.js
+++ b/plugins/htmlisp-renderer/mod.js
@@ -1842,6 +1842,21 @@ var plugin = {
           }
           case "getComponents":
             return { result: pluginContext.components };
+          case "getRenderContext": {
+            const { components: components2, componentUtilities, globalUtilities } = pluginContext;
+            const runtimeUtilities = getRuntimeUtilities({
+              componentUtilities,
+              globalUtilities,
+              matchRoute: payload.route,
+              url: payload.url
+            });
+            return {
+              result: {
+                components: components2,
+                componentUtilities: runtimeUtilities.componentUtilities,
+                utilities: runtimeUtilities.utilities
+              }
+            };
+          }
           case "getComponentDependencyGraph":
             return {
               result: {
diff --git a/plugins/og/mod.js b/plugins/og/mod.js
index 0000000..0000000 100644
--- a/plugins/og/mod.js
+++ b/plugins/og/mod.js
@@ -1081,7 +1081,7 @@ var plugin = {
   init: async ({ options: { layout }, load, outputDirectory }) => {
     const ogLayout = await load.textFile(layout);
     return {
-      beforeEachRender: async ({ url, context, send }) => {
+      beforeEachRender: async ({ url, route, context, send }) => {
         if (!url.endsWith(".html") && !url.endsWith(".html/") && !url.endsWith(".xml") && !url.endsWith(".xml/")) {
           const meta = toRecord(await send("gustwind-meta-plugin", {
             type: "getMeta",
@@ -1089,15 +1089,21 @@ var plugin = {
           }));
-          const components = await send("htmlisp-renderer-plugin", {
-            type: "getComponents",
-            payload: void 0
-          });
+          const renderContext = toRecord(await send("htmlisp-renderer-plugin", {
+            type: "getRenderContext",
+            payload: { route, url }
+          }));
+          const { components, componentUtilities, utilities } = renderContext;
           const svg = await htmlispToHTML({
             htmlInput: ogLayout,
             // @ts-expect-error It would be better to type this somehow but this will do
             components,
+            // @ts-expect-error It would be better to type this somehow but this will do
+            componentUtilities,
+            // @ts-expect-error It would be better to type this somehow but this will do
+            utilities,
             context: {
               ...context,
               meta: { ...meta, ...toRecord(context.meta) }
